server:
  port: 8123
  servlet:
    context-path: /api
    session:
      cookie:
        max-age: 2592000
spring:
  application:
    name: wlc-picture-backend
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/my_picture
    username: root
    password: wlc040305
  profiles:
    active: local

  servlet:
    multipart:
      max-file-size: 10MB

  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    timeout: 5000
  session:
    store-type: redis
    timeout: 2592000
#    # 空间图片分表
#    shardingsphere:
#      datasource:
#        names: my_picture
#        my_picture:
#          type: com.zaxxer.hikari.HikariDataSource
#          driver-class-name: com.mysql.cj.jdbc.Driver
#          url: jdbc:mysql://127.0.0.1:3306/my_picture
#          username: root
#          password: wlc040305
#      rules:
#        sharding:
#          tables:
#            picture:
#              actual-data-nodes: my_picture.picture # 动态分表
#              table-strategy:
#                standard:
#                  sharding-column: spaceId
#                  sharding-algorithm-name: picture_sharding_algorithm  # 使用自定义分片算法
#          sharding-algorithms:
#            picture_sharding_algorithm:
#              type: CLASS_BASED
#              props:
#                strategy: standard
#                algorithmClassName: com.wlc.picture.manager.sharding.PictureShardingAlgorithm
#      props:
#        sql-show: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    enable-sql-runner: true
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名
      logic-delete-value: 1 # 逻辑已删除值（默认为 1）
      logic-not-delete-value: 0 # 逻辑未删除值（默认为 0）
# 接口文档配置
knife4j:
  enable: true
  openapi:
    title: "接口文档"
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.wlc.picture.controller
